package com.mygdx.towerdefence.client.serverCommands;

import com.badlogic.gdx.utils.JsonValue;

public class UpgradeBuildingServerCommand implements ServerCommand {
    @Override
    public void execute(JsonValue msg) {
    }
}
